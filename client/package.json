{
  "name": "chat-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "peerjs": "^0.3.24",
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-scripts": "2.1.5",
    "socket.io-client": "^2.2.0",
    "socket.io-p2p": "^2.2.0"
  },
  "scripts": {
    "tailwind:css": "tailwind build src/tailwind.css.src -c  tailwind.js -o src/tailwind.css",
    "start": "npm run tailwind:css && react-scripts start",
    "build": "npm run tailwind:css && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "proxy": "http://localhost:9000",
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "tailwindcss": "^0.7.4"
  }
}
